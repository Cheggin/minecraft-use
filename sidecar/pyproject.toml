[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
    "test_convex_upload.py",
    "test_download.py",
    "browser_search.py",
    "server.py",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
